dash
dash[diskcache, flask]
dash-daq
dash-bootstrap-components
polars
pyarrow
h5py
numpy
pandas
kaleido
diskcache
# Already pulled in by dash[diskcache], but named here because settings picks
# the start method background callback workers are created with.
multiprocess
orjson
# Serves the desktop app on loopback behind the native window.
waitress
# Native window shell. Uses the OS webview: WebView2 on Windows, WKWebView on
# macOS, and on Linux either WebKitGTK or QtWebEngine -- neither of which comes
# with pywebview, so a Linux run needs one of them installed alongside it or the
# app falls back to opening the default browser:
#   `pip install qtpy PySide6`             -- Qt, self-contained
#   `pip install "pywebview[gtk]"`         -- GTK, plus system gir1.2-webkit2-4.1
# Not listed as a dependency here because the choice is per-platform; the Linux
# CI build installs the Qt pair so the built app carries its own renderer.
pywebview
typing-extensions
# Bundles a static ffmpeg, used to transcode camera streams a browser cannot play.
imageio-ffmpeg
